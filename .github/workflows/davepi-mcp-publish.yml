name: 'Publish @davepi/mcp'

# Publishes the @davepi/mcp package to npm when a tag matching
# `@davepi/mcp@<version>` is pushed. The tag's <version> must equal
# the package.json's `version` field — the workflow checks this and
# fails fast on mismatch so a stale tag doesn't publish the wrong
# bits.
#
# Auth: classic NPM_TOKEN via the `npm-publish` GitHub Environment.
# Trusted Publishing (OIDC) is the longer-term path but isn't yet
# available on this npm account; until then a granular access token
# with write scope on this package lives in the env secret as
# NPM_TOKEN, gated behind the `npm-publish` environment so a reviewer
# approves each release.

on:
  push:
    tags:
      - '@davepi/mcp@*'

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: npm-publish
    defaults:
      run:
        working-directory: packages/mcp
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20.x
          registry-url: 'https://registry.npmjs.org'

      - name: Verify tag matches package.json version
        env:
          GITHUB_REF_NAME: ${{ github.ref_name }}
        run: |
          set -e
          # Tag format: @davepi/mcp@<version>
          tag_version="${GITHUB_REF_NAME##*@}"
          pkg_version=$(node -p "require('./package.json').version")
          if [ "$tag_version" != "$pkg_version" ]; then
            echo "::error::tag version ($tag_version) does not match package.json version ($pkg_version)"
            exit 1
          fi
          echo "Publishing @davepi/mcp@$pkg_version"

      - name: Run package tests before publish
        run: npm test

      - name: Publish to npm
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npm publish --access public
